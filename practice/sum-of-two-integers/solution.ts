// LeetCode 371: Sum of Two Integers
// Constraint: Do not use + or - operators
/**
 * Given two integers a and b, return their sum without using the
 * + or - operators. Use bit manipulation instead.
 *
 * Example 1: a = 1, b = 2 -> 3
 * Example 2: a = 2, b = 3 -> 5
 * Example 3: a = -1, b = 1 -> 0
 *
 * Constraints:
 * - Must not use + or - operators
 */
export function getSum(a: number, b: number): number {
  // TODO: implement using bit manipulation
  return 0
}
