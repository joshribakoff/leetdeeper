// Decode Ways - Leetcode 91
// https://leetcode.com/problems/decode-ways/

/**
 * A message of letters is encoded to numbers: 'A'=1, 'B'=2, ..., 'Z'=26.
 * Given a string s of digits, return the number of ways to decode it.
 *
 * Example 1: s = "12" -> 2 ("AB" as 1,2 or "L" as 12)
 * Example 2: s = "226" -> 3 ("BZ", "VF", "BBF")
 * Example 3: s = "06" -> 0 (leading zero is invalid)
 *
 * Constraints:
 * - '0' cannot be decoded on its own
 * - Valid two-digit codes are 10-26
 */
export function numDecodings(s: string): number {
  // TODO: implement
  return 0
}
