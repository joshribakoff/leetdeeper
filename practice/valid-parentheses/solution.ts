// Valid Parentheses - Leetcode 20
// Given a string containing just '(', ')', '{', '}', '[' and ']',
// determine if the input string is valid.
/**
 * Given a string s containing just the characters '(', ')', '{', '}',
 * '[' and ']', determine if the input string is valid. Brackets must
 * close in the correct order and each close bracket must have a
 * matching open bracket of the same type.
 *
 * Example 1: s = "()" -> true
 * Example 2: s = "()[]{}" -> true
 * Example 3: s = "(]" -> false
 * Example 4: s = "([])" -> true
 */
export function isValid(s: string): boolean {
  // TODO: implement
  return false
}
