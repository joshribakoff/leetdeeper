// LeetCode 57: Insert Interval
// Insert a new interval into sorted non-overlapping intervals, merging if necessary.

/**
 * Given a sorted array of non-overlapping intervals and a new interval, insert
 * the new interval and merge overlapping intervals. Return the result sorted.
 *
 * Example 1: intervals = [[1,3],[6,9]], newInterval = [2,5] -> [[1,5],[6,9]]
 * Example 2: intervals = [[1,2],[3,5],[6,7],[8,10],[12,16]], newInterval = [4,8] -> [[1,2],[3,10],[12,16]]
 *
 * Constraints:
 * - Input intervals are sorted by start and non-overlapping
 * - Merge any overlapping intervals after insertion
 */
export function insert(intervals: number[][], newInterval: number[]): number[][] {
  // TODO: implement
  return []
}
