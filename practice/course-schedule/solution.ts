// Course Schedule - Leetcode 207
// https://leetcode.com/problems/course-schedule/

/**
 * Given numCourses and an array of prerequisite pairs [a,b] meaning you must
 * take course b before course a, return true if you can finish all courses.
 *
 * Example 1: numCourses = 2, prerequisites = [[1,0]] -> true
 * Example 2: numCourses = 2, prerequisites = [[1,0],[0,1]] -> false (cycle)
 *
 * Constraints:
 * - Detect if a cycle exists in the course dependency graph
 */
export function canFinish(numCourses: number, prerequisites: number[][]): boolean {
  // TODO: implement
  return false
}
